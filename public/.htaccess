RewriteEngine On

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

# ALTERNATIVE: Erweiterte Version mit Static Asset Optimierung
# Uncomment für bessere Performance:

# # Static Assets direkt servieren (ohne PHP)
# RewriteCond %{REQUEST_URI} \.(css|js|png|jpg|jpeg|gif|webp|svg|woff|woff2|ttf|otf|eot|ico|pdf|zip)$ [NC]
# RewriteRule ^.*$ - [L]
#
# # API Routes markieren für spezielle Behandlung
# RewriteCond %{REQUEST_URI} ^/api/ [NC]
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ index.php [QSA,L,E=REQUEST_TYPE:api]
