# Football Manager Environment Configuration
# Local Development Settings - FIXED

# Application
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8000
CDN_URL=

# Database - Main Connection (FIXED: consistent database name)
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=football_manager
DB_USERNAME=root
DB_PASSWORD=

# Database - Read Replicas (optional, fallback to write connection)
DB_READ_HOST_1=localhost
DB_READ_PORT_1=3306
DB_READ_USERNAME_1=root
DB_READ_PASSWORD_1=

# Database - Connection Testing (disabled for faster startup)
DB_TEST_ON_BOOT=false

# Email Configuration (Use local SMTP or disable for development)
MAIL_DRIVER=mail
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=
MAIL_FROM_ADDRESS=noreply@localhost
MAIL_FROM_NAME="Football Manager Local"

# Email Queue (disabled for development)
MAIL_QUEUE_ENABLED=false
MAIL_QUEUE_BATCH_SIZE=1
MAIL_QUEUE_RETRY_ATTEMPTS=1
MAIL_QUEUE_RETRY_DELAY=60

# Session
SESSION_DOMAIN=
SESSION_SECURE=false

# Logging
LOG_LEVEL=info
LOG_RETENTION_DAYS=7

# Game Settings
REGISTRATION_ENABLED=true

# Security (use dummy values for development)
JWT_SECRET=development-jwt-secret-key-change-in-production

# CORS (allow all for development)
CORS_ALLOWED_ORIGINS=*

# CDN (disabled for development)
CDN_ENABLED=false

# Monitoring (disabled for development)
METRICS_ENABLED=false